<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cocacola.mvc.mapper.BrandStoryMapper">
    <select id="selectAll" resultType="cocacola.mvc.domain.BrandStoryDTO">
        SELECT 
            brand_story_id AS id,
            brand_story_title AS title,
            brand_story_summary AS summary,
            brand_story_image AS image,
            brand_story_date AS "date"
        FROM BRAND_STORY 
        ORDER BY brand_story_id ASC
    </select>

    <select id="selectById" parameterType="int" resultType="cocacola.mvc.domain.BrandStoryDTO">
        SELECT 
            brand_story_id AS id,
            brand_story_title AS title,
            brand_story_summary AS summary,
            brand_story_image AS image,
            brand_story_date AS "date"
        FROM BRAND_STORY 
        WHERE brand_story_id = #{id}
    </select>
</mapper>