[ 테이블 ]
CREATE TABLE OURCOMPANY (
    CONTENT_ID      NUMBER          PRIMARY KEY,       -- 고유 식별자 (SEQUENCE 사용 권장)
    SECTION_CODE    VARCHAR2(50)    NOT NULL,          -- 섹션 구분 코드 (예: GLOBAL_COKE, KOREA_COKE)
    TITLE           VARCHAR2(255)   NOT NULL,          -- 제목 (<h2>에 출력)
    SUMMARY         VARCHAR2(1000)  NOT NULL,          -- 요약 내용 (<p>에 출력)
    IMAGE_FILENAME  VARCHAR2(255)   NOT NULL,          -- 이미지 파일 이름 (경로 제외)
    BUTTON_LINK     VARCHAR2(255)   DEFAULT '#',       -- '더 보기' 버튼 링크 URL
    DISPLAY_ORDER   NUMBER          DEFAULT 1          -- 출력 순서 (정렬 기준)
);

CREATE SEQUENCE SEQ_OURCOMPANY START WITH 1 INCREMENT BY 1;


------------------------

[ 속성 ]
CONTENT_ID          각 콘텐츠를 고유하게 식별하는 기본 키 (PK).
SECTION_CODE        어떤 콘텐츠인지 구분하며, **default-order**나 reverse-order 클래스를 동적으로 결정하는 JSTL 조건문(test="${globalItem.SECTION_CODE == 'GLOBAL_COKE'}")에 사용. 카테고리 같은 역할.
TITLE               큰 제목 텍스트.
SUMMARY             본문 요약 텍스트.
IMAGE_FILENAME      이미지 파일 이름만 저장. (예: OurCompany_cocacola_company_01.png) JSP에서 IMAGE_BASE_PATH와 조합하여 완전한 URL을 생성한다.
BUTTON_LINK         '더 보기' 버튼 클릭 시 이동할 URL
DISPLAY_ORDER       JSP에서 데이터를 조회할 때 순서를 결정하기 위한 값.
