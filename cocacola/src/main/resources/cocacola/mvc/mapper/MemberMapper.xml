<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cocacola.mvc.mapper.MemberMapper">
	<select id="login" resultType="cocacola.mvc.domain.MemberDTO">
		SELECT * FROM tbl_member
		WHERE userid = #{userid} AND userpw = #{userpw}
	</select>

	<insert id="insertMember"
		parameterType="cocacola.mvc.domain.MemberDTO">
		INSERT INTO tbl_member (userid, userpw, username, email, phone)
		VALUES (#{userid}, #{userpw}, #{username}, #{email}, #{phone})
	</insert>

	<select id="idCheck" resultType="int">
		SELECT COUNT(*) FROM tbl_member WHERE userid = #{userid}
	</select>
	
<select id="findByIdAndEmail" parameterType="cocacola.mvc.domain.MemberDTO" resultType="cocacola.mvc.domain.MemberDTO">
    SELECT * 
    FROM tbl_member
    WHERE userid = #{userid} AND email = #{email}
</select>

<!-- 전화번호로 회원 조회 -->
<select id="findByPhone" parameterType="cocacola.mvc.domain.MemberDTO" resultType="cocacola.mvc.domain.MemberDTO">
    SELECT *
    FROM tbl_member
    WHERE REPLACE(phone, '-', '') = #{phone}
</select>


</mapper>