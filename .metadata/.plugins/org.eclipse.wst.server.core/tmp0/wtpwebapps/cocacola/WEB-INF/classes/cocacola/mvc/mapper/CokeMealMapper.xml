<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cocacola.mvc.mapper.CokeMealMapper">
    <select id="selectAll" resultType="cocacola.mvc.domain.CokeMealDTO">
        SELECT 
            coke_meal_id AS id,
            coke_meal_title AS title,
            coke_meal_summary AS summary,
            coke_meal_image AS image,
            coke_meal_date AS "date"
        FROM COKE_MEAL 
        ORDER BY coke_meal_id ASC
    </select>
</mapper>